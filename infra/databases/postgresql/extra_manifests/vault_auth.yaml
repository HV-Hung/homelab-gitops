apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: default
  namespace: databases
spec:
  vaultAuthGlobalRef:
    allowDefault: true
    namespace: vault
  kubernetes:
    serviceAccount: vso-sa
    role: default
