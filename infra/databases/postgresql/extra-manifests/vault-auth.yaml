apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: default
  namespace: databases
spec:
  method: kubernetes
  kubernetes:
    serviceAccount: vso-sa
    role: default
  vaultAuthGlobalRef:
    allowDefault: true
    namespace: vault
