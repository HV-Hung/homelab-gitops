apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: default
  namespace: cloudflare
spec:
  method: kubernetes
  kubernetes:
    serviceAccount: vso-sa
    role: cloudflare
  vaultAuthGlobalRef:
    allowDefault: true
    namespace: vault
