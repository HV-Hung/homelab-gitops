apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-secrets-operator
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-4"
spec:
  project: infra
  sources:
    - repoURL: https://github.com/HV-Hung/homelab-gitops
      targetRevision: main
      ref: values
    - repoURL: https://helm.releases.hashicorp.com
      chart: vault-secrets-operator
      targetRevision: "1.3.0"
      helm:
        valueFiles:
          - $values/infra/vault/operator/values.yaml
    - repoURL: https://github.com/HV-Hung/homelab-gitops
      targetRevision: main
      path: infra/vault/operator/extra-manifests
    
  destination:
    server: https://kubernetes.default.svc
    namespace: vault
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
