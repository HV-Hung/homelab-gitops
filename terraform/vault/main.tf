terraform {
  required_providers {
    vault = {
      source  = "hashicorp/vault"
      version = "~> 3.0"
    }
  }
}

provider "vault" {
  # VAULT_ADDR and VAULT_TOKEN should be set in the environment
}
